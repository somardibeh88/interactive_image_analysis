from setuptools import setup, find_packages

setup(
    name='imageanalysis',
    version='0.1',
    packages=find_packages(),  # Automatically finds the imageanalysis folder
    install_requires=[],
)
